; ====================================================================
; NAP TELECOM - ARI CONFIGURATION (Asterisk REST Interface)
; ====================================================================

[general]
enabled = yes
pretty = yes
allowed_origins = *

[nap_admin]
type = user
read_only = no
password = nap_ari_secret_2026
password_format = plain
